# build output
dist/
# generated types
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
*.log


# environment variables
.env
.env.production

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/

# SDD 진행 원장 (스크래치 작업공간)
.superpowers/

# 개인 문서 — 이 저장소는 공개이므로 커밋되면 누구나 내려받을 수 있다.
# 이력서·포트폴리오 원본을 참고용으로 폴더에 두더라도 절대 올라가지 않게 한다.
# 사이트에 실을 이미지는 public/ 아래에 두면 되므로 루트 패턴으로 제한한다.
/*.pdf
/*.doc
/*.docx
/*.hwp
/*.hwpx
